# Use the LAN address of the host (or the HTTPS ngrok gateway URL) on physical devices.
# Both HTTP and WebSocket traffic must reach the same Nginx gateway.
EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:8080
EXPO_PUBLIC_WS_URL=http://YOUR_LAN_IP:8080
